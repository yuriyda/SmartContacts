# Google Setup

OAuth client configuration and required scopes will be documented here when
Plan P4 (Device Sync) introduces the Google Drive `appdata` scope, and updated
again when Plan P5 (Google Contacts integration) introduces the `contacts`
scope and the People API mapping.

Until then, no Google OAuth client is required to run the app — the browser
shell works entirely against the local wa-sqlite/IndexedDB store.
