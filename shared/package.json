{
  "name": "@smart-contacts/shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "lint": "eslint 'src/**/*.{ts,tsx}'",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -b"
  },
  "dependencies": {
    "wa-sqlite": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^1.5.0",
    "@vitest/coverage-v8": "^1.5.0",
    "fake-indexeddb": "^5.0.0",
    "jsdom": "^24.0.0"
  }
}
